# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=traycer_try
MONGODB_MAX_POOL_SIZE=10
MONGODB_MIN_POOL_SIZE=1
MONGODB_MAX_IDLE_TIME_MS=30000
MONGODB_SERVER_SELECTION_TIMEOUT_MS=5000
MONGODB_CONNECT_TIMEOUT_MS=10000

# Google Gemini API
# Never commit real API keys. Use a secure secret manager in production.
# WARNING: Replace with your actual API key, but never commit it to version control
GEMINI_API_KEY=your_api_key_here
GEMINI_TEMPERATURE=0.1
GEMINI_MAX_TOKENS=1000
GEMINI_MAX_CONTENT_LENGTH=4000
GEMINI_MAX_SIMILARITY_CONTENT_LENGTH=1000

# Agent Configuration
AGENT_TIMEOUT_SECONDS=30
PARSER_TIMEOUT_SECONDS=45
CATEGORIZER_TIMEOUT_SECONDS=30
PROCESSOR_TIMEOUT_SECONDS=60
AGENT_MAX_RETRIES=3
AGENT_CONFIDENCE_THRESHOLD=0.7

# Scraper Configuration
SCRAPER_CONCURRENCY=5
SCRAPER_REQUEST_TIMEOUT_SECONDS=20
SCRAPER_DELAY_SECONDS=1.0
SCRAPER_USER_AGENT=TrayceAI-Bot/1.0
SCRAPER_RESPECT_ROBOTS=true
SCRAPER_MAX_RETRIES=3
SCRAPER_MAX_REDIRECTS=5
SCRAPER_CONTENT_SIZE_LIMIT=10485760

# Processing Configuration
PROCESSING_TIMEOUT_SECONDS=60
PROCESSING_MAX_RETRIES=2
PROCESSING_CONCURRENCY=3
ENABLE_CONTENT_CLEANING=true
ENABLE_AI_ANALYSIS=true
ENABLE_SUMMARIZATION=true
ENABLE_STRUCTURED_EXTRACTION=true
ENABLE_DUPLICATE_DETECTION=true
SIMILARITY_THRESHOLD=0.8
MIN_CONTENT_QUALITY_SCORE=0.4
MAX_SUMMARY_LENGTH=500
PROCESSING_BATCH_SIZE=10
CONTENT_PROCESSING_TIMEOUT=30
MAX_CONCURRENT_AI_ANALYSES=5
MAX_PROCESSING_MEMORY=512
PROCESSING_MAX_SIMILARITY_CONTENT_PAIRS=50
PROCESSING_MAX_SIMILARITY_BATCH_SIZE=10

# Database Settings
# Database operation timeouts in seconds (5-300) - Controls MongoDB query timeout
DATABASE_QUERY_TIMEOUT_SECONDS=30
# Maximum retries for failed database operations (0-10) - Exponential backoff retry attempts
DATABASE_MAX_RETRIES=3
# Batch size for bulk database operations (1-1000) - Optimizes bulk insert/update performance
DATABASE_BATCH_SIZE=100
# Enable full-text search features - MongoDB text search capabilities
DATABASE_ENABLE_TEXT_SEARCH=true
# Automatic content cleanup TTL in days (1-3650) - Data retention policy for content
DATABASE_CONTENT_TTL_DAYS=90
# Analytics data retention period in days (30-3650) - Long-term analytics storage
DATABASE_ANALYTICS_RETENTION_DAYS=365
# Enable query result caching - Performance optimization through result caching
DATABASE_ENABLE_CACHING=true
# Cache expiration time in seconds (60-86400) - TTL for cached query results
DATABASE_CACHE_TTL_SECONDS=3600
# Maximum individual document size in MB (1-1000) - Prevents oversized documents
DATABASE_MAX_CONTENT_SIZE_MB=50
# Enable content compression for storage - Reduces storage footprint
DATABASE_ENABLE_COMPRESSION=true
# Create indexes in background mode - Non-blocking index creation
DATABASE_INDEX_BACKGROUND=true
# Enable query profiling for development - Performance monitoring and debugging
DATABASE_ENABLE_PROFILING=false

# Health Check Settings
HEALTH_AGENT_TEST_TIMEOUT=5
HEALTH_PROCESSING_TEST_TIMEOUT=8

# Application Settings
ENVIRONMENT=development
LOG_LEVEL=INFO
DEBUG=true
SECRET_KEY=change_me_in_production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
